function randomperm(n::Int64)
    # Generate a random permutation, σ
end

function get_descents(perm::Vector)
    # Given a permutation, return the set of indices such that σ(i)>σ(j)
end

function TrueDPP(n)
    # Returns the true DPP given by Borodin 2009
end